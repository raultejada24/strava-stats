# Optional: default path to your Strava export .zip, so you don't have to
# pass --export every time. You can always override it with:
#   python fetch/sync.py --export /otra/ruta/export_12345.zip
STRAVA_EXPORT_PATH=/ruta/a/tu/export_12345.zip
